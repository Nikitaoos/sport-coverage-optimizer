numpy>=1.24.0
# Опциональные зависимости (используются если установлены, иначе встроенные замены):
# pulp>=2.7.0          # решатель ЦЛП (заменяется встроенным жадным алгоритмом)
# shapely>=2.0.0       # геометрия полигонов (заменяется numpy-реализацией)
# scikit-learn>=1.3.0  # K-means (заменяется встроенной реализацией)
