numpy>=1.24.0

# Опциональные зависимости (используются, если установлены, иначе — встроенные замены):
# pulp>=2.7.0          # точный решатель ЦЛП (замена: встроенный жадный алгоритм)
# scikit-learn>=1.3.0  # K-means (замена: встроенная реализация Lloyd's algorithm)
# matplotlib>=3.7.0    # визуализация сетки и маршрутов (tools/plot_scenario.py)
